[package]
name = "interop"
version = "0.1.0"
edition = "2021"
description = "MossyMesh interop: TWAMM, OpenAPI gateway, liquidity mining"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
